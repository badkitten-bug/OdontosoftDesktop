{
  "name": "odontosoft-main",
  "version": "1.0.0",
  "main": "main.js",
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "jest": "^30.2.0"
  },
  "description": "Backend Electron para OdontoSoft Desktop",
  "scripts": {
    "pretest": "npm rebuild better-sqlite3 || echo 'Rebuild skipped (may already be built)'",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "type": "commonjs",
  "dependencies": {
    "archiver": "^7.0.1",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^9.2.2",
    "electron-log": "^5.4.3",
    "electron-updater": "^6.8.3"
  }
}
